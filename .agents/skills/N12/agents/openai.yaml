interface:
  display_name: "N12"
  short_description: "Repo-specific patterns and workflows for N12"
  default_prompt: "Use the N12 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true